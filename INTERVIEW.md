# 面试准备手册（企业知识库 RAG 项目）

> 面试前把「一、自我介绍」背熟，其余部分理解到「能用自己的话讲一遍」即可。

---

## 一、项目自我介绍（1 分钟版，直接背）

> 我做过一个「企业知识库智能问答」项目，用的是 **LangChain + DeepSeek + 向量数据库**。
>
> 核心功能是：把公司的制度文档（PDF、Word 这些）上传进去，系统先把文档**切分、向量化**存到向量库里；用户提问时，系统先**语义检索**出相关的内容，再交给大模型**基于这些内容生成回答**，而不是让大模型凭空编。
>
> 我重点做了六块优化：
> 第一，**多轮对话**——用户追问「那请假呢」这种省略的问题，我会先用大模型把它**改写成完整问题**再检索；
> 第二，**检索优化**——切分时按标题结构切、还给每块注入章节标题，检索时向量 + 关键词混合召回再重排，正确率从 25% 提到了 100%；
> 第三，**防幻觉 + 引用高亮**——用系统提示词约束大模型别编造，还让它在回答里标引用编号，能点回到具体原文；
> 第四，**Agent 化**——让大模型自己判断要不要查资料，像「你好」这种问候就直接回答，不用浪费检索；
> 第五，**对话历史持久化**——用 SQLite 把会话和消息存下来，刷新页面不丢，还能新建 / 切换 / 删除多个会话；
> 第六，**用户登录**——用 JWT 做登录认证，密码加盐哈希存，每个用户只能看到自己的会话；
> 第七，**用户画像 / 长期记忆**——每轮聊完自动提取用户透露的个人信息存成画像，下次提问时注入，让 AI 跨会话记住用户是谁。

---

## 二、一句话讲清楚 RAG（被问「什么是 RAG」时）

> RAG 就是「检索增强生成」。大模型有两个毛病：一是**知识有时效**，二是**会一本正经地胡说**（幻觉）。
> RAG 的思路是：不让大模型只靠自己背的东西回答，而是先去一个**外部知识库**里检索相关内容，把这些内容塞给大模型当参考，让它「照着资料回答」。这样回答更准、还能溯源。

**记一个比喻**：把大模型想象成一个很聪明但记性不好的学生，RAG 就是考试时给他一本「开卷资料」，他照着资料答，就靠谱了。

---

## 三、项目亮点（面试时主动讲，别等面试官问）

1. **混合检索 + 重排（两阶段检索）**——这是全文最有含金量的点
2. **结构感知切分**——按标题切 + 标题继承，块自包含章节信息
3. **多轮对话的问题改写**（指代消解）
4. **防幻觉 + 引用高亮**
5. **Agent 化查询路由**——让大模型自主判断是否检索
6. **对话历史持久化**——SQLite 存会话，刷新不丢
7. **用户登录（JWT）**——密码加盐哈希，多用户会话隔离
8. **用户画像 / 长期记忆**——自动提取用户信息存画像，跨会话记住用户

---

## 四、高频面试题 + 通俗参考答案

### Q1：RAG 的完整流程是什么？
> 分两段。
> **离线索引**：文档 → 解析出文字 → 切成小块（chunk）→ 每块转成向量 → 存进向量库。
> **在线问答**：用户提问 → 语义检索出最相关的几个块 → 拼成上下文 → 交给大模型生成回答。

### Q2：为什么要切分（chunk）？怎么切更聪明？
> 两个原因：一是整篇文档太长，塞不进大模型的上下文窗口；二是切小后检索更精准，不会因为一个词命中就把整篇不相干的文档捞回来。
> 我一开始用 RecursiveCharacterTextSplitter 硬按字符数切，发现它会为了凑字符数把相邻章节合并到一起（「请假」和「报销」挤进同一块），检索时互相污染。
> 后来改成**结构感知切分**：识别标题行（##、一、二、三、）当切分边界，按章节切，再把章节标题注入到每个块里。这样每块自带「属于哪个章节」，检索和生成时上下文都完整。

### Q3：Embedding（向量化）是什么？为什么要用它？
> 就是把一段文字变成一个数字向量，**语义相近的文本，向量也相近**。这样「升职」和「晋升」虽然字不一样，但向量很接近，就能搜到。
> 我用的 Embedding 模型是 bge-m3。

### Q4：为什么用向量数据库？
> 因为要按「语义」检索，不是按关键词。传统数据库只能精确匹配关键词，搜「涨工资」就搜不到「薪资调整」。向量数据库存的是向量，能做「最相似的 k 个」这种近似检索。

### Q5：怎么防止大模型胡说八道（幻觉）？怎么做到可溯源？
> 三层：一是系统提示词强制它「只能根据资料回答，没有就说找不到」；二是只把**检索到的内容**给它当上下文，让它没东西可编；三是**引用高亮**——让大模型在回答里标引用编号 [1][2]，前端渲染成可点击的上标，点一下就能看到对应的原文片段。

### Q6：多轮对话是怎么做的？（⭐ 高频）
> 难点是「指代消解」：用户问「那请假呢」，这个「那」指什么，系统得知道。
> 我的做法是：如果带了历史，就先用大模型把当前问题**改写成独立完整的问题**，比如「那请假呢」→「请假超过3天需要谁审批？」，然后再去检索。改写完的问题照样返回给前端，让用户看到系统理解对了没。

### Q7：说说你的检索方案？（混合检索 + 重排）（⭐⭐⭐ 最高频）
> 我的检索分两阶段：**召回 + 重排**。
>
> **召回用「混合检索」**：向量检索 + BM25 关键词检索各召回一批，再用 RRF 融合。
> 为什么混合？两者各有短板——向量懂语义（「涨工资」能搜到「薪资调整」），但容易漏精确关键词（「5000」「财务总监」）；BM25 精确匹配强，但不懂同义词。结合就互补了。
>
> **重排**：召回只是粗筛，可能不够准，所以再用重排模型逐字精读问题和候选，重新排序取 top-4。
>
> 效果：12 道题评估，纯向量 25% → 混合检索 100% → 混合 + 重排 100%。

### Q8：怎么评估效果的？
> 我构造了 12 个「问题 + 期望关键词」的测试集，用关键词匹配判断答对没答对，对比了三种方案：纯向量 25%，混合检索 100%，混合检索 + 重排 100%。我会说明这个评估比较粗略，但趋势能说明每层优化的价值。

### Q9：遇到最大的困难是什么？（⭐ 必问）
> 三个。
> 第一是**检索不准**：章节之间主题相近（都讲「谁审批」），纯向量检索经常把正确章节漏掉。最后靠「混合检索 + 重排」解决。
> 第二是**多轮改写套模板**：改写模型会把上一句的句式照搬过来（问「那报销呢」它答成「报销需要满足什么条件」），导致检索跑偏。后来我在改写提示词里加了「不要照搬句式」的规则和示例，就修好了。
> 第三是**切分把列表项误判成标题、内容丢失**：部署上线后，用户上传了一个「迟到罚款」的 txt，问「迟到扣多少钱」却检索不到。排查发现文档里 `1.五分钟扣50` 这种数字列表项，被结构感知切分的「标题识别」误判成了章节标题，而标题行后面没有正文，于是整行内容被丢弃，库里只剩一个「##迟到」的块。最后修了切分逻辑：让标题行本身也保留进正文，内容就不再丢了。

### Q10：为什么选 DeepSeek，不选 GPT？
> 一是便宜、国内直连方便；二是它提供 OpenAI 兼容接口，代码里用 LangChain 的 ChatOpenAI 换个 base_url 就能接，切换成本很低。

### Q11：Agent 化是怎么做的？（查询路由）
> 传统 RAG 是「无脑检索」：不管问什么，哪怕是「你好」，都先去查一遍知识库，浪费还容易跑偏。
> 我做了一个**查询路由**：先用大模型判断「这个问题要不要查资料」。问候、闲聊、感谢这类，直接友好回答、跳过检索；涉及公司制度、流程的，才走「改写 → 检索 → 生成」的完整流程。
> 这一步就是 Agent 最基础的形态——把「要不要查资料」这个决策权交给大模型。前端还会显示「🔎 检索知识库」或「💬 直接回答」的标记，让用户看到它的决策。

### Q12：对话历史是怎么持久化的？
> 前端原来把历史存在内存数组里，一刷新就没了，而且只能开一个会话。
> 我改成用 **SQLite** 持久化，建了两张表：`conversations`（会话）和 `messages`（消息，用 conversation_id 外键关联到会话）。
> 流程是：提问时前端带上 conversation_id，后端先从数据库读历史拼成多轮上下文，回答完再把「用户问题 + 助手回答」写回消息表；同时提供会话的增删查接口，前端做成侧边栏，能新建、切换、删除会话。
> 为什么选 SQLite？它是 Python 内置的，零依赖、单文件、够轻量，适合这种演示项目；如果生产要并发多用户，可以换 PostgreSQL。

### Q13：用户登录是怎么做的？（JWT）
> 两个核心：**密码安全**和**登录态管理**。
> 密码安全：不能明文存，我用 **PBKDF2 加盐哈希**——注册时给每个用户随机生成一个盐，把「盐 + 密码」哈希后存库。这样即使数据库泄露，攻击者也拿不到明文；而且两个用户密码相同、哈希也不同，没法用彩虹表批量破解。
> 登录态：用 **JWT（JSON Web Token）**。登录成功后，后端把用户 ID + 过期时间打包成一串带签名的 token 发给前端；之后每次请求前端在 `Authorization` 头里带上它，后端验签就能确认身份，不用每次查数据库存 session（无状态）。
> 多用户隔离：会话表加了 user_id，查会话、查消息、删除时都按 user_id 过滤，所以用户 A 看不到、也访问不了用户 B 的会话（越权访问会返回 404）。

### Q14：用户画像 / 长期记忆是怎么做的？
> 这是为了解决「多轮对话只记得当前会话、换个会话就忘了用户是谁」的问题——让 AI 像人一样「长期记住」用户。
> 做法分两步：
> **写入**：每轮聊完后，单独调一次大模型当「画像提取器」，判断用户这轮有没有透露值得长期记住的信息（姓名、职位、部门、偏好这些稳定信息），有就合并进画像存到 users 表；临时的一次性信息（「今天想请假」）不记。这一步我放在后台线程做，不阻塞正常回答。
> **注入**：下次提问时，把画像拼进系统提示词，这样哪怕全新会话、没有历史，AI 也能说出「你叫王小明，在技术部」。
> 为什么不用「把所有历史都喂给 AI」这种简单做法？因为历史会越来越长、超出上下文窗口，而且混着大量无关对话；画像是把信息「蒸馏」成一句稳定的结论，轻量又精准。

---

## 五、可能被追问的细节

- **Embedding 为什么用硅基流动？** → DeepSeek 没有 Embedding 接口，硅基流动提供 bge-m3 的云端 API，不用本地跑模型。
- **向量库为什么选 Chroma？** → 轻量、本地持久化、免费，适合演示和学习；生产可以用 Milvus / Weaviate。
- **chunk_overlap 是什么？** → 相邻块之间的重叠长度，防止一句话被切在边界上断掉语义。
- **如果文档更新了怎么办？** → 重新索引（先清空 collection 再重新切分向量化）。
- **为什么用 FastAPI？** → Python 生态和 LangChain 天然契合，FastAPI 轻量、异步、自动生成接口文档。
- **BM25 是什么？** → 经典的关键词检索算法，按词频/逆文档频率算相关度，精确匹配强、但不懂语义。
- **RRF 是什么？** → 倒数排名融合，把多个排序列表合并：每篇文档得分 = Σ 1/(k + 排名)，k 常取 60。
- **中文 BM25 怎么分词？** → 用 jieba，否则 BM25 默认按空格切分，中文整句会被当成一个词。
- **引用高亮怎么实现？** → 后端给每个检索块编号，系统提示词要求大模型在回答里用 [编号] 标注；前端用正则把 [n] 替换成高亮上标，点击滚动到对应来源卡片。
- **流式输出怎么实现？** → 后端用 FastAPI 的 StreamingResponse + 大模型的 stream() 边生成边返回；前端用 fetch 读 ReadableStream 逐段追加。检索是一次性做完的，只有生成是流式的。
- **查询路由怎么实现？** → 单独调一次大模型，让它把问题分类成「需要检索 / 不需要检索」，代码按结果走两个分支：不需要检索就走友好的闲聊提示词直接答；需要检索才走「改写 + 混合检索 + 重排」。有个坑要留意——「不需要检索」里包含「需要」这个子串，判断时得先排除「不需要」再判断「需要」。
- **标题继承是什么？** → 切分时把章节标题拼进每个块的开头，让块自带「属于哪个章节」的上下文，避免检索出来的内容变成孤立的句子。
- **切分怎么把列表项误判成标题的？** → 结构感知切分靠正则识别「标题行」当切分边界（##、一、1. 开头都算标题），但 `1.五分钟扣50` 这种「数字列表项」和 `1. 总则` 这种「数字章节标题」长得太像，正则分不清。一旦列表项被当成标题，它后面又没有正文，整行就被丢弃。修法是「标题行本身也保留进正文」——这样即使误判，内容也不会丢。这个 bug 是上线后用户传真实文档才暴露的，让我意识到「切分要保证内容零丢失，宁可结构识别保守一点」。
- **纯向量检索为什么这么低（25%）？** → 纯语义检索对「精确数字、专有名词」（5000、90天、财务总监）不敏感，这些要靠 BM25 关键词检索命中。切分越细、块越专一，纯向量越容易漏，反而更说明必须用「混合检索 + 重排」。
- **为什么对话历史用 SQLite 不用 MySQL/Postgres？** → 因为要的是「零依赖、单文件、够用」。SQLite 是 Python 内置的 sqlite3，不用装数据库服务，本地一个 .db 文件就能跑，特别适合演示和轻量项目。它的短板是写入是单写的、并发有限，真要上生产、多用户同时读写，就换 PostgreSQL。
- **JWT 和 session 有什么区别？** → session 是「有状态」：后端把登录态存服务器（内存或数据库），每次请求查一下；JWT 是「无状态」：信息全在 token 里，后端验签就行，不存东西，更适合分布式/多服务。缺点是 JWT 发出后不好主动作废（除非加黑名单），所以一般配短过期时间。
- **密码哈希为什么用 PBKDF2 不用 MD5/SHA1？** → MD5/SHA1 算得太快，攻击者能一秒试几百万个密码暴力破解；PBKDF2 故意慢（迭代几万次），让暴力破解成本高到不划算。更专业的场景还会用 bcrypt / argon2。
- **怎么防止用户 A 访问用户 B 的会话？** → 两层：一是会话表存 user_id，查询时 `WHERE user_id = ?` 过滤，列表天然只返回自己的；二是拿别人会话 ID 来查时，后端先校验「这个会话是否属于当前用户」，不属于就返回 404（而不是 403，避免暴露「这个 ID 存在」）。
- **长期记忆和对话历史有什么区别？** → 对话历史是「短期记忆」，只在这个会话里有效，切会话就忘；用户画像是「长期记忆」，跨会话、跨登录都有效。历史是流水账，画像是蒸馏出的稳定结论。

---

## 六、面试时的一句话收尾

> 这个项目虽然规模不大，但把 RAG 的核心链路——切分、向量化、检索、生成——都走了一遍，还额外做了结构感知切分、多轮对话、混合检索 + 重排、引用高亮、流式输出、Agent 化查询路由、对话历史持久化、用户登录这几个优化。如果进公司，我最想继续深入的方向是更精细的效果评估、Docker 上线部署、用向量数据库的分片/分布式能力支撑更大规模，让系统更接近生产可用。
